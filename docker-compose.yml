version: "3.3"
services:
  compile-kernel:
    volumes:
      - ./out:/out
      - ./cadmium:/cadmium
    image: compile-kernel
    stdin_open: true # docker run -i
    tty: true        # docker run -t
networks: {}
